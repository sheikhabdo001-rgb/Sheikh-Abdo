window.VIEW_TEMPLATES = {
    home: window.HOME_VIEW,
    students: window.STUDENTS_VIEW,
    studentCodes: window.STUDENT_CODES_VIEW,
    schedule: window.SCHEDULE_VIEW,
    payments: window.PAYMENTS_VIEW,
    monthDisplay: window.MONTH_DISPLAY_VIEW,
    attendance: window.ATTENDANCE_VIEW,
    records: window.RECORDS_VIEW,
    profile: window.PROFILE_VIEW,
    nonPayers: window.NON_PAYERS_VIEW,
    groupAttendanceArchive: window.GROUP_ATTENDANCE_ARCHIVE_VIEW,
    repeatedAbsence: window.REPEATED_ABSENCE_VIEW,
    financialReports: window.FINANCIAL_REPORTS_VIEW,
    exams: window.EXAMS_VIEW,
    examGrades: window.EXAM_GRADES_VIEW,
    backup: window.BACKUP_VIEW,
    transfer: window.TRANSFER_VIEW
};

// removed inline template strings for home, students, schedule, payments, monthDisplay, attendance, records, profile, and nonPayers views.
